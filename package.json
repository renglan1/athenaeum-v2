{
  "name": "athenaeum-v2",
  "private": true,
  "scripts": {
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && gridsome build",
    "develop": "SET NODE_OPTIONS=--openssl-legacy-provider && gridsome develop",
    "explore": "gridsome explore"
  },
  "dependencies": {
    "gridsome": "^0.7.0"
  },
  "devDependencies": {
    "sass": "^1.52.3",
    "sass-loader": "^10.1.1",
    "webpack-node-externals": "^3.0.0"
  }
}
